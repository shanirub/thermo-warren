# Secrets -- never commit. Wi-Fi credentials live in NVS on the device;
# this CSV is the host-side input used to generate the NVS image, and it
# holds the password in cleartext. wifi_creds.csv.example carries the shape.
wifi_creds.csv
wifi_creds.bin

# Generated by idf.py; sdkconfig.defaults is the committed source of truth.
sdkconfig
sdkconfig.old

# Build output
build/

# idf.py add-dependency / component manager state
managed_components/
dependencies.lock
